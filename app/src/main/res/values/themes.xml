<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MainApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="color">@color/white</item>
        <item name="background">@drawable/gradient</item>
        <item name="tickColor">@color/gradient_2</item>
        <item name="trackColor">@color/gradient</item>
        <item name="colorOnBackground">@color/white</item>
        <item name="colorError">@color/dark</item>
        <item name="colorOnError">@color/color2</item>
    </style>

    <style name="Theme.MainApp" parent="Base.Theme.MainApp" />

</resources>